import type { FormEvent } from "react";
import { ArrowRight, Sun } from "lucide-react";
import { formatTodayDate, getGreeting } from "../home-formatters";

export function TodayCaptureWidget({ firstName, captureText, todayCount, isLoading, onCaptureTextChange, onSubmit }: { firstName: string; captureText: string; todayCount: number; isLoading: boolean; onCaptureTextChange: (value: string) => void; onSubmit: (event: FormEvent<HTMLFormElement>) => void }) {
  return <div className="flex min-w-0 flex-col justify-center"><p className="flex items-center gap-2 text-sm font-semibold text-indigo-600 dark:text-indigo-300"><Sun className="h-4 w-4" />{formatTodayDate()}</p><h1 className="mt-2 text-3xl font-semibold text-slate-950 dark:text-white sm:text-4xl">{getGreeting()}, {firstName}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-600 dark:text-slate-300 sm:text-base">{isLoading ? "Gathering today's memories..." : todayCount ? `You captured ${todayCount} ${todayCount === 1 ? "memory" : "memories"} today.` : "Your day is ready for its first memory."}</p><form onSubmit={onSubmit} className="enterprise-card mt-5 flex items-end gap-2 p-2 shadow-[0_14px_32px_rgba(15,23,42,0.08)]"><label htmlFor="today-capture" className="sr-only">Quick capture</label><textarea id="today-capture" rows={2} value={captureText} onChange={(event) => onCaptureTextChange(event.target.value)} placeholder="What do you want to remember about today?" className="min-h-16 min-w-0 flex-1 resize-none bg-transparent px-3 py-2 text-sm leading-6 text-slate-900 outline-none placeholder:text-slate-400 dark:text-slate-100" /><button type="submit" disabled={!captureText.trim()} className="action-primary h-11 w-11 shrink-0 p-0 disabled:opacity-40" aria-label="Continue memory in Diary"><ArrowRight className="h-5 w-5" /></button></form></div>;
}
